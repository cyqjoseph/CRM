# Centralised Resource Manager (CRM)

A serverless app that discovers, tracks and renews expiring credentials —
TLS/x.509 certificates and AWS IAM access keys — with a Cognito-authenticated
self-service dashboard and a full audit trail.

Generated by the BRD Architect platform. The approved specification lives in
`spec/` (`requirements.md`, `design.md`, `tasks.md`) and the BRD reference
documents in `docs/`; both are inputs and are never deployed. This README
describes what was actually built. Pushes to `main` trigger a redeploy.

## What it does

**Certificates** are discovered from IAM server certificates and Secrets Manager
entries tagged `crm:resource-type=certificate`. **IAM access keys** are
discovered by scanning IAM users for active keys, flagging any older than 90 days
(`warning`) or 180 days (`critical`).

Both inventories are scanned hourly against three expiry thresholds (30/14/7
days). Each match fans out to an SNS topic by severity *and* to an SQS queue for
Jira ticket creation — independently, so a failure in one never suppresses the
other. Every lifecycle event is appended to a 90-day-TTL audit table and streamed
to S3 before it expires (7 years for IAM-account events, 3 years for certs).

From the dashboard an owner can see their own certs and accounts, trigger a
renewal or rotation, and request a password reset for an account. Password reset
requests land in an approval queue rather than executing; an admin approves or
rejects them from an admin-only tab. Admins can also query the audit trail for
any entity.

## Architecture

```
CloudFront ──> S3 (static UI)          EventBridge (daily / hourly)
     │                                        │
     └── browser ── Cognito (SRP) ──┐         ├──> discovery-sfn ──> discovery-acm-fn
                                    │         │                      discovery-iam-fn
                            API Gateway       └──> expiry-evaluator-fn ──> SNS (low/med/high)
                        (Cognito authorizer)                          └──> SQS ──> jira-notifier-fn
                                    │                                              └──> DLQ
        ┌───────────────────────────┼───────────────────────────┐
        │              │            │             │             │
   api-certs-fn   api-iam-fn   api-audit-fn  api-password-  api-health-fn
        │              │            │         resets-fn      (unauthenticated)
        │              │            │             │
   renewal-sfn    rotation-sfn      │      password-reset-approver-fn
        │              │            │             │
  renewal-        rotation-         │      password-reset-sfn
  executor-fn     iam-key-fn        │        └──> password-reset-executor-fn
        │              │            │
        └──────────────┴────────────┴──> DynamoDB ──stream──> audit-exporter-fn ──> S3
```

**Tables** — `cert-inventory` (GSIs: `OwnerIndex`, `ExpiryIndex`),
`iam-accounts` (`OwnerIndex`, `StatusIndex`), `audit-hot` (TTL + stream),
`password-reset-requests` (`StatusIndex`).

**Async actions.** `POST /certs/{id}/renew` and `POST /iam/accounts/{id}/rotate`
return `202` with an `executionArn` the moment the state machine starts. The
outcome arrives only from `GET /executions/{executionArn}`, which the UI polls to
a terminal state — see *Renew and rotate report their outcome* below.

All infrastructure is in `template.yaml` (AWS SAM). Shared Lambda code lives in
`layers/common/python/crm_common/`.

## Running it

```bash
./deploy.sh                    # build, deploy, seed demo rows, publish the UI
./scripts/validate.sh          # end-to-end smoke test of the real browser path
./destroy.sh                   # tear everything down
```

`deploy.sh` is idempotent and writes `outputs.json` (including `app_url`) at the
repo root. It also invokes both discovery Lambdas once so the tables have data
immediately instead of waiting for the daily schedule.

`validate.sh` exercises exactly what the browser does — CloudFront → Cognito
sign-in → ID token → API Gateway authorizer → Lambda → DynamoDB — plus the CORS
preflight, the unauthenticated 401, owner scoping, and a full renew (start, poll
to `SUCCEEDED`, and confirm the row's `ExpiryDate` actually moved). It exits
non-zero on the first failure and names the failing state.

Both scripts resolve stack outputs via `scripts/lib-stack-outputs.sh`, preferring
a local `outputs.json` and falling back to CloudFormation. The fallback is the
normal path: this project deploys on push, so `deploy.sh` runs on the build host
and `outputs.json` is gitignored.

### Tests

```bash
pip install -r requirements-dev.txt
python3 -m pytest -q
cfn-lint template.yaml
```

One file per Lambda (mocking `boto3` — no `moto`, no live AWS), plus
`test_template.py` for structural/IAM invariants, `test_scripts.py` for
`deploy.sh`/`destroy.sh`, and `test_ui_async_actions.py` for the polling
behaviour above.

## Things that will confuse you

### The permissions boundary denies more than the IAM policies suggest

Every role carries `brd-architect-deploy-boundary`, which the platform owns and
this template only references. Effective permissions are the *intersection* of a
role's policy and that boundary, and the boundary only allows the services in
`CLAUDE.md`'s allowed-services list.

**ACM is not on that list.** So `acm:*` is denied no matter what a role grants.
Granting it anyway is worse than useless: the deploy succeeds, and the call fails
at runtime deep inside a state machine where the `AccessDeniedException` reaches
only the execution history. That is precisely how the Renew button came to return
`202` and then do nothing at all.

Nothing in this repo calls ACM any more, and
`test_template.py::test_no_acm_permissions_anywhere_in_the_template` keeps it
that way. **If a call is boundary-denied, remove the call — do not add the
permission.**

### Renew and rotate report their outcome

Because both endpoints return `202` immediately, a UI that stops there cannot
tell success from failure. `ui/app.js`'s `runAsyncAction` starts the action,
polls `GET /executions/{executionArn}` to a terminal status, and then either
relabels the button and reloads the table (so the new `ExpiryDate`/`Status`
shows) or surfaces the failing state name. A stuck execution times out rather
than disabling the button forever.

### An empty Certificates tab is normal, not a fault

Discovery and the API disagree about what `OwnerId` means:

| | writes/reads `OwnerId` as |
|---|---|
| `discovery_acm` (IAM server certs) | the certificate's `Path` |
| `discovery_acm` (Secrets Manager) | the `crm:owner-id` tag |
| `discovery_iam` | the IAM user's `Path` |
| `GET /certs`, `GET /iam/accounts` | the caller's Cognito `sub` (a UUID) |

Both index the same `OwnerIndex` GSI, so a perfectly healthy discovery run writes
rows that belong to no human login and appear in **nobody's** dashboard.

`deploy.sh` therefore seeds demo rows against `SEED_OWNER_ID` (override it with
your own Cognito `sub`). Reconciling the two meanings properly — mapping a
discovered resource to an owning user — is left open.

Two traps make hand-seeding error-prone: `ExpiryDate` and `NextRotationDate` are
`OwnerIndex`'s RANGE key, and DynamoDB silently omits an item lacking one from
the index (accepted on write, never returned by `GET /certs`); and the IAM tab
renders `Status` and `UserName`, not `RotationStatus`.

### Secrets Manager's recovery window blocks the next deploy

`DeleteStack` does not purge a secret — it schedules deletion behind a 30-day
window that keeps the *name* reserved. The next `CREATE` of that name fails
immediately, and because `JiraTokenSecret` is dependency-free it sits in
CloudFormation's first creation wave, so its failure stamps every table, bucket,
topic and queue beside it with "Resource creation cancelled" and the real reason
appears nowhere. `deploy.sh` purges such secrets before deploying (restore first
— `--force-delete-without-recovery` is rejected on an already-scheduled secret),
`destroy.sh` purges them after `DeleteStack`, and a failed deploy dumps the root
stack failures with the cancellation noise filtered out.

### Other sharp edges

- **`MfaConfiguration` must stay quoted.** Bare `OFF` is a YAML 1.1 boolean
  alias, so CloudFormation sends the string `false` and Cognito rejects it.
  `test_template.py` guards every such alias in the template.
- **The common layer has no `Metadata: BuildMethod`.** That would route it
  through SAM's pip builder, which needs a `requirements.txt` this layer does not
  have; if `python/` is lost from the artifact root every function fails init on
  `from crm_common import ...`, which API Gateway reports only as `502`.
- **Unhandled exceptions must not become 502s.** API Gateway answers a raising
  proxy-integration Lambda with a bare `502` carrying no CORS headers, so the
  browser reports a CORS error and the UI says "Failed to fetch".
  `crm_common.guard_api_handler` turns those into a well-formed `500`. The
  event-driven functions deliberately keep raising — Step Functions retries and
  SQS redrive-to-DLQ depend on it.
- **State machine roles need their own X-Ray grant.** SAM injects X-Ray
  permissions only into roles it generates; these state machines supply their
  own `Role`, so tracing was enabled but unauthorised, failing silently.
- **`logs:*` is granted with `Resource: "*"` on purpose.** A function's log group
  does not exist until its first invocation creates it, so a policy scoped to
  that group's ARN denies the very call that would create it.

## Deviations from `spec/`

The spec assumed a `{env}-crm-*` naming convention and a higher-trust
network/identity environment than this account provides. Where they conflict,
`CLAUDE.md`'s platform contract wins.

- **Naming** — every resource uses the mandated `app-d9fae51c-1929cc69-` prefix.
- **Auth** — Cognito self-signup (email/password, no MFA) instead of SAML
  federation; there is no external IdP here to federate with.
- **On-prem AD replaced with AWS IAM discovery** — there is no network path to
  on-prem from this account and no Directory Service in the allowed-services
  list. `discovery-iam-fn` scans real IAM access keys through the same Step
  Functions/DynamoDB/API shape the AD design had.
- **Certificate renewal records rather than reissues** — `renewal-executor-fn`
  makes no ACM calls (see above). It extends the inventory row's `ExpiryDate` by
  one validity period, resets `Status` to `ISSUED` and bumps `Version`. Real
  issuance needs a validated domain this project does not have.
- **IAM key rotation flags rather than mutates** — `rotation-iam-key-fn` makes
  zero IAM calls. Automatically deactivating a live access key is
  high-blast-radius and irreversible: anything still using it breaks instantly.
  It records `mode: "NOTIFY_ONLY"`; a human completes the rotation out of band.
- **Password reset generates but never delivers** — `password-reset-executor-fn`
  generates a temporary credential, stores only its SHA-256 hash plus metadata in
  Secrets Manager, and discards the plaintext, which is never returned, logged or
  persisted (policy C-02). SES is not an allowed service, so there is no
  permitted channel to deliver it; the SNS notification says the reset happened,
  not what the password is.
- **Owner scoping is enforced in application code**, comparing a resource's
  `OwnerId` to the JWT's `sub`, rather than via IAM `LeadingKeys` conditions.
  A single Lambda execution role sits behind API Gateway (no Identity Pool, no
  per-user credentials), so `LeadingKeys` has no per-caller identity to key off.
- **CORS `AllowOrigin` is `*`** — the CloudFront domain is AWS-generated, so
  pinning it would make the API depend on the distribution and serialise every
  deploy behind a CloudFront rollout. The Cognito authorizer is the security
  boundary; no cookies or credentialed requests are used.
- **Jira integration ships with a placeholder token.** `JiraTokenSecret` holds
  `REPLACE_ME`, so ticket creation fails until it is set. Failures land in the
  DLQ by design rather than blocking the alert path.

## Layout

```
template.yaml            all infrastructure (AWS SAM)
deploy.sh / destroy.sh   the platform's fixed entry points
functions/               one directory per Lambda
layers/common/           shared crm_common helpers
statemachines/           Step Functions ASL definitions
ui/                      static SPA (no build step)
scripts/                 validate.sh + stack-output resolution
tests/                   pytest suite
spec/ docs/ design/      platform inputs — reference only, never deployed
```
