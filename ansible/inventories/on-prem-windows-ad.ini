# STUB — example only. Not used by any deploy/test/build step in this repo.
#
# TODO: replace with real domain controller hostnames and a real credential
# lookup (Ansible Vault or your own secrets manager) before ever running this.

[domain_controllers]
dc1.example-corp.internal
dc2.example-corp.internal

[domain_controllers:vars]
ansible_connection=winrm
ansible_winrm_transport=kerberos
ansible_user=CHANGE_ME
# TODO: never commit a real password here — source it from Ansible Vault.
ansible_password=CHANGE_ME
