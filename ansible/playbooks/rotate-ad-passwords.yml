# STUB — non-functional. See ansible/README.md.
---
- name: Rotate a flagged on-prem AD account password
  hosts: domain_controllers
  gather_facts: false
  roles:
    - rotate-ad-passwords
    - report-to-dynamodb
